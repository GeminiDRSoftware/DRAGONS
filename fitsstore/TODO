- add phoenix data

- change logging so that it doesn't include the system name

- debug open database sessions

- email warning if ingestqueue gets too deep

- emails allerts for data on programs.

- Check GNIRS data (this is more an AD descriptors thing), but still needs doing

- NIRI darks

- make fullheader text have info section at top.

- handle non-present files properly

Features:
 - Duplicate data label detection
 - option to ingest old files from an alternate dir with present=false

 - decide about GSA interaction.

 - write curation script that checks for missing entries etc

 - fix cadcCRC and wmd classes to handle ctrl-c s properly.

- ingest reduced calibration data
- consider reduced data files too
- does one DB need to handle multiple data stores?

- database backup cronjob

- hyperlinks to fits files
- jpeg previews of FITS files...

Database schema changes:

- add a text column in diskfile that contains a full header text dump.

Apachehandler:

Summary Generator:
create a history summary

Logistics:
comment, document and mark authorship on code


low priority:
investigate parsing eObslog to get comments.
any way to get pre-imaging flag from odb? anything else we need? requestd conditions
completely customisable columns in summary.

