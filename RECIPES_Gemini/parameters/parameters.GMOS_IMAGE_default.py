#
from astrodata.ParamObject import PrimitiveParameter

localParameterIndex = {'theta':PrimitiveParameter('theta', 4, True, help="For testing."), 
                      'sigma':PrimitiveParameter('sigma', 8, False, help="For testing.")
                      }
