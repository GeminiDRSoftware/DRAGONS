#
from astrodata.ParamObject import PrimitiveParameter

localParameterIndex = {'theta':PrimitiveParameter('theta', 4, True), 
                      'sigma':PrimitiveParameter('sigma', 8, False)
                      }
