# Set up the nici reduction scripts into this pyraf environment.
# The 'nicipath' (pyraf) environment must be defined at this moment
# and need to end with '/' (old iraf legacy).

pyexecute("nicipath$nici_pyraf.py")
